# Database Configuration
# Para PostgreSQL local usa este formato:
# DATABASE_URL=postgresql://usuario:password@localhost:5432/nombre_database
DATABASE_URL=

# Server Configuration
HOST=0.0.0.0
PORT=1337

# Secrets - IMPORTANTE: Genera valores únicos y seguros para producción
# Puedes generarlos con: openssl rand -base64 32
APP_KEYS=
API_TOKEN_SALT=
ADMIN_JWT_SECRET=
TRANSFER_TOKEN_SALT=
JWT_SECRET=
